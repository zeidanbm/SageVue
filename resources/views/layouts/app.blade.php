@include('partials.header')


<div id="app"></div>
{{-- <div class="container">
    <main class="main">
        @yield('content')
        
    </main>

    @hasSection('sidebar')
        <aside class="sidebar">
            @yield('sidebar')
        </aside>
    @endif
</div> --}}

@include('partials.footer')
